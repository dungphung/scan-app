import { Divider } from "./divider";
import { Loading } from "./loading";
import { Text } from "./text";
import { Header } from "./header";
import { Icon, IIconProps } from "./icon";

export { Divider, Loading, Text, Header, Icon };

export default {
  Divider,
  Loading,
  Text,
  Header,
  Icon,
};

export type { IIconProps };
